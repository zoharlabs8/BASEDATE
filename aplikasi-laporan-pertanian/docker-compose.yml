version: "3.8"
services:
  zetapp:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./sample:/app/sample
    env_file:
      - .env
    restart: unless-stopped
    # scale untuk 5+ user: docker-compose up --scale zetapp=2
